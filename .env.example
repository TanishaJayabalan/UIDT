# Database configuration
DB_NAME=traffic_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# Redis (task queue)
REDIS_URL=redis://redis:6379

# Environment mode
ENV=development
USE_MOCK_DATA=true

# Email mode (no real emails in dev)
EMAIL_MODE=dev
